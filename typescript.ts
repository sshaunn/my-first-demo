function sum(a, b) {
    return a + b;
}
console.log(sum(1, 1));


let isBool: boolean = true;

let test: string = 'test';

let basket: [string, number];
basket = ['basketball', 5];

let error = (): string => {
    throw Error('ops');
}