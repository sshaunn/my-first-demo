class Task {
    constructor(id, taskName, taskType, status ) {
            this.id = id;
            this.taskName = taskName;
            this.taskType = taskType;
            this.status = status;
    }
}

module.exports = Task;